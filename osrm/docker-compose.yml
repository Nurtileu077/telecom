services:
  osrm:
    image: osrm/osrm-backend:latest
    # MLD: те же данные, что подготовил setup.sh (osrm-partition + osrm-customize).
    command: osrm-routed --algorithm mld /data/region.osrm
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    restart: unless-stopped
